# opengrok
**OpenGrok — An original OpenAI project**   > A clean, authentic, and 100% local interface for the Grok CLI, featuring full agentic autonomy, native web access, and universal connectivity to any `llama-server` or Ollama instance with full CORS support (`*`).
